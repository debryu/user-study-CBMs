# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Notebooks
.ipynb_checkpoints/

# Claude Code local session state and maintainer notes
/.claude/
CLAUDE.md

# Dataset and generated artifacts (distributed via HuggingFace instead)
# unanchored so this matches inside every experiment folder (cub/data/, emails/data/, ...)
data/
clip_embeddings/
*_csv/

# Scratch/local-only files (e.g. downloaded Drive results), not tracked
temp/
utils/
